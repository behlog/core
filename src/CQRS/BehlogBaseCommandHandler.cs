using Microsoft.Extensions.Logging;

namespace Behlog.Core;

public class BehlogBaseCommandHandler
{
    private readonly ILogger<BehlogBaseCommandHandler> _logger;

    public BehlogBaseCommandHandler(ILogger logger)
    {
        
    }
}